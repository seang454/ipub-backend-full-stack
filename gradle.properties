# JVM memory for Gradle (fix daemon crash)
org.gradle.jvmargs=-Xms512m -Xmx2g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8

# Speed up build
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true

# Limit test workers (important for VM)
org.gradle.test.worker.max=2
